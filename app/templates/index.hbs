{{outlet}}

<div class="layout-row layout-align-center-start">
  <div class="flex-50">
    {{unstagram-timeline
      photos=photos
      page=page
      getPhotos=(action "getPhotos")
    }}
  </div>
</div>
